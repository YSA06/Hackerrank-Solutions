function sides(literals, ...expressions) {
    let a=expressions[0];
    let p=expressions[1];
    let arr =[];
    arr.push((p+Math.sqrt(p*p-16*a))/4);
    arr.push((p-Math.sqrt(p*p-16*a))/4);
    return arr.sort();

}
